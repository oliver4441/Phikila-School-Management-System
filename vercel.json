{
  "framework": "vite",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install --registry=https://registry.npmjs.org/",
  "rewrites": [{ "source": "/((?!static/).*)", "destination": "/index.html" }]
}
