# Firebase Auth is used for sign-in (email/password and Google). All three
# VITE_FIREBASE_* values are Firebase *publishable* client config (bundled
# into the browser); never put secrets here.
VITE_FIREBASE_API_KEY=AIzaSyAs7C-OegYfoPxj8LOYNagZgcMi9yo45Zg
VITE_FIREBASE_AUTH_DOMAIN=omix-systems-cd1af.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=omix-systems-cd1af
# The Cloudflare Worker backend. Required because the frontend (Vercel) and
# backend (Workers) are on different origins.
VITE_API_URL=https://phikila-backend.kipkiruigideon890.workers.dev